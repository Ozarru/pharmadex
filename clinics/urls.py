from django.urls import path
from .views import *

urlpatterns = [
    # path("dashboard/", clinics_dashboard, name="clinics-dashboard"),
    # path("inventory-dashboard/", inventory_dashboard, name="inventory-dashboard"),
    # path("report/", clinics_reports, name="clinics-reports"),
    # path("analytics/", clinics_analytics, name="clinics-analytics"),
    # path("point-of-sale/", point_of_sale, name="point-of-sale"),
    # path("prescription-queue/", prescription_queue, name="prescription-queue"),
    # path("profit-and-loss-reports", point_of_sale, name="profit-and-loss-reports"),

    # # Pharmacy URLs
    # path('clinics/list/', PharmacyListView.as_view(), name='clinics-list'),
    # path('clinics/create/', PharmacyCreateView.as_view(), name='clinics-create'),
    # path('clinics/<uuid:pk>/detail/', PharmacyDetailView.as_view(), name='clinics-detail'),
    # path('clinics/<uuid:pk>/update/', PharmacyUpdateView.as_view(), name='clinics-update'),
    # path("clinics/analytics/", clinics_analytics, name="clinics-analytics"),

]
